import { NewIterationDirective } from './new-iteration.directive';

describe('NewIterationDirective', () => {
  it('should create an instance', () => {
    const directive = new NewIterationDirective();
    expect(directive).toBeTruthy();
  });
});
